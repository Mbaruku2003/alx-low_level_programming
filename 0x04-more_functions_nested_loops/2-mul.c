#include "main.h"
/**
 * mul - multiplies integers
 * @a: an integer being multiplied
 * @b: an integer being multiplied
 * Return: 0(success)
 */
int mul(int a, int b)
{
	int  sum = a * b;

	return (sum);
}
