#include <stdio.h>
#include <stdlib.h>
/**
 * mul - multiplies two integers
 * @a: the first int to b multiplied
 * @b: the second integer to be multiplied
 * Return: mul
 */
int mul(a, b)
{
	product = a * b;
	return (product);
}
