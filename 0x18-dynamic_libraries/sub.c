#include <stdlib.h>
#include <stdio.h>
/**
 * sub - subtracts b from a
 * @a: the number to be subtracted from
 * @b: the number that is being subtracted
 * Return: subtract
 */
int sub(int a,int b)
{
	return (a - b);
}
