# include <stdio.h>
/**
 * Main - Entry point of the program
 * Return; Always 0 (success)
 */
int main(void)
{
	char (c);

	c = 'z';

	while
		(c >= 'a')
		{
			putchar (c);
			c--;
		}

			return (0);
}
