#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result
 * @a: one of the integers being added
 * @b: another integer being added
 * Result: result(success)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
