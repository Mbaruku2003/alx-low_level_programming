#include "main.h"
#include <stdio.h>
/**
 * _islower - checks for lowercase character
 * Return: 1 (if c is lower), 0 (otherwise)
 */
int _islower(int c)
{
	char c;

	for (c = 97; c <= 122; c++)
	{
		return (1)
	}
	return (0);
}
