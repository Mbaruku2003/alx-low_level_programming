#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	if (n > 0)
	{
		printf("%d is positive\n", i);
	}
	else if (n == 0)
	{
		printf("%d is zero\n", i);
	}
	else
	{
		printf("%d is negative\n", i);
	}
	return (0);
}
